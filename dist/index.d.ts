import * as React from 'react';
import React__default, { FC, ReactNode } from 'react';

interface Props {
    children: ReactNode | ReactNode[];
}
declare const Button: FC<Props>;

declare const Typography: () => React__default.JSX.Element;

declare function BasicTable(): React.JSX.Element;

export { BasicTable, Button, Typography };
